import 'package:flutter/material.dart';

class SeatSelect extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //Code Here
      body: Center(child: Text("TODO --> SEAT SELECTION MODEL"))
    );
  }
}